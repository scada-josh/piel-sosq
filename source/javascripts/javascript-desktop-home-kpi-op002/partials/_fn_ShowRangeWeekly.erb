ShowRangeWeekly: function (startDate, endDate, startWeekNo, endWeekNo, year) {
	

	// console.log("start date : " + start.format('MM/DD/YYYY'));
	// console.log("end date : " + end.format('MM/DD/YYYY'));

	// fn.paramStartDate =  start.format('MM/DD/YYYY');
	// fn.paramEndDate = end.format('MM/DD/YYYY');

	fn.paramStartDate =  startDate;
	fn.paramEndDate = endDate;
	
	// console.log('ShowRangeDate');
	// console.log("site name : " + fn.paramSiteName);
	// console.log("start date : " + fn.paramStartDate);
	// console.log("end date : " + fn.paramEndDate);

  	$('#reportrange span').html(startDate.format('DD/MM/YYYY') + ' - ' + endDate.format('DD/MM/YYYY'));

  	// fn.HeaderFilter(fn.paramSiteName, start.format('DD/MM/YYYY'), end.format('DD/MM/YYYY'));
  	fn.HeaderFilterWeekly(fn.paramSiteName, startWeekNo, endWeekNo, year);
  
  
},
